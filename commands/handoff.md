---
description: Save or restore a CTXORA conversation handoff.
argument-hint: save <summary> | restore <id>
---

Use CTXORA handoff MCP tools. For `save`, call `handoff_conversation` with a concise summary of decisions, changed files, unresolved risks, and next steps. For `restore`, call `restore_conversation_handoff` and verify the restored context before continuing.

Request: $ARGUMENTS
