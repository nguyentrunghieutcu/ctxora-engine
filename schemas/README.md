# MCP API v2 schemas

This directory contains the complete request, response, and typed-error JSON Schema contract for every public MCP v2 tool. The files are generated from harness_context.api.v2.contracts and verified by the API contract tests.
