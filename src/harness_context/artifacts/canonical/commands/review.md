---
description: Review a change using repository context and targeted evidence.
argument-hint: [scope or task]
---

Use CTXORA `retrieve_context` to locate the relevant implementation, tests, and conventions. Review the requested scope for correctness, regressions, security, and missing tests. Report findings by severity with absolute file paths and line numbers.

Scope: $ARGUMENTS
