---
description: Navigate CTXORA skills, commands, roles, profiles, and MCP capabilities.
argument-hint: [task]
---

Use the `ctxora-navigation` skill and `docs/COMMAND-SKILL-MAP.md` to route this request to one primary workflow. Explain the selected skill, optional command or role, and whether any capability must be installed separately.

Task: $ARGUMENTS
